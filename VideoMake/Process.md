## VideoMake process

1. Fill the video information into `constants.ts`.
2. Set the playback order of the video components in `VideoAllOrder.tsx`.
3. Run `npm run dev` and make adjustments for inappropriate and error cases.
4. While the test is successful, export the video.